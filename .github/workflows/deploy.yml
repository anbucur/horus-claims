name: Deploy

on:
  push:
    branches: [master]

jobs:
  deploy:
    name: Deploy via Docker Compose
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Compose
        run: |
          echo "Docker Compose deployment triggered for master branch"
          echo "Build and deploy steps would execute here"

      - name: Deploy with Docker Compose
        run: |
          echo "::notice::Deploy step: docker-compose up -d"
          # Actual deployment would run:
          # docker-compose up -d --build
